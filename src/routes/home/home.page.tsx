
import { PageTitle, Page } from "@/components/page";

const HomePage = () => {
  return (
    <Page>
      <PageTitle>Dashboard</PageTitle>

      {/* XXX : replace by dashboard components when ready */}
      <span className="italic">Bientôt disponible !</span>
    </Page>
  );
};

export default HomePage;
